# 2026-05-19 Work Log

## Workspace Synchronization Convention

- Local development directory: `/home/slh/4_28/RTIP`
- Remote run directory: `~/RTIP` on `ssh n5`
- Development workflow: modify code and documentation locally; real tests, job submission, and code execution are completed on `n5:~/RTIP`.
- File transfer: after local changes are complete, synchronize them to `n5:~/RTIP` with `scp`.
- Synchronization principle: keep source code and documentation consistent; keep the remote runtime environment and cached artifacts, such as `.venv/`, `.pytest_cache/`, `__pycache__/`, and `*.egg-info/`, as remote runtime state.

## Today's Changes

- Confirmed that the local repository and remote repository have the same git HEAD: `d07b0c4d9d3dae42c2cf9edf50a578aaeff16a5e`.
- Confirmed that the only local untracked file is `.codex`; this file belongs to local Codex session state and is not synchronized as project source code.
- Added this file to record development, synchronization, and test-run information for 2026-05-19.
- Synchronized this file to `n5:~/RTIP/20260519.md` and verified that the local and remote contents match with `sha256sum`.
